# 2026-05-31

- 对项目进行了全面的功能和代码质量分析，产出了详细的评估报告（协议合规性、代码结构、可维护性等维度）。
- 编写了中文版 README.md，包含功能概述、报文格式说明、硬件要求、环境配置、编译烧录方法、配置参数说明、验证方式等。
- 完成了全面重构，将单文件 615 行拆分为 5 个模块：
  - `crid_config.h/c`：配置结构体和初始化
  - `crid_messages.h/c`：报文构建（Basic ID / Location / System + Beacon 帧打包）
  - `crid_wifi.h/c`：Wi-Fi 初始化和四级 fallback 发送
  - `crid_patrol.h/c`：巡游位置模拟
  - `crid-sim.c`：精简后的主入口（~90 行）
- 修复的问题：
  - 地速编码改用浮点运算，消除 uint8_t 截断 bug
  - 气压高度移除 `esp_random()` 随机抖动
  - `strcpy` → `strncpy`，增加帧缓冲区溢出检查
  - 发送任务用 `vTaskDelayUntil` 替代轮询
  - `flash_and_monitor.sh` 路径改为环境变量配置
  - `CMakeLists.txt` 和 README.md 同步更新
- 根据接收装置反馈修复两个问题：
  1. 增加操作员 ID：在配置中增加 `operator_id` 字段（默认 `OP-CAAC-ESP32-001`），新增 Self-Description 报文（0x3）通过打包消息传输，打包消息数量从 3 条增至 4 条
  2. 修复操作员高度编码精度：System 报文中操作员高度从 cm 精度改回 0.5m 精度（`(alt + 1000) * 2`），与原始实现一致
- 根据接收软件截图全面修复报文格式，使其符合 ASTM F3411 / OpenDroneID 标准：
  1. 修复 Basic ID 报文：字节1位序改为 [UAType(4)][IDType(4)]，UAS ID 填充从空格改为 NULL
  2. 修复 Location 报文：字节1增加 SpeedMult/HeightType 位域，高度编码从 cm 改为 0.5m 精度，字节19/20改为 [VertAccuracy][HorizAccuracy] 和 [BaroAccuracy][SpeedAccuracy]，时间戳改用 UTC 时间计算，垂直速度编码修正为 (speed*2 + 63)
  3. 修复 System 报文：字节1改为 [Reserved(3)][ClassificationType(3)][OperatorLocationType(2)]，区域高度改用 0.5m 精度（解决 31767.5m 溢出），字节17改为 [ClassEU(4)][CategoryEU(4)]
  4. 增加 Authentication 报文（0x2）和 Operator ID 报文（0x5），打包消息从 4 条增至 6 条
  5. 增加配置字段：operator_location_type, classification_type, category_eu, class_eu, height_type
  6. 编译验证通过
- 根据第二个接收软件截图进一步修复：
  1. 修复航迹角编码：从 1°/LSB 改为 0.5°/LSB，符合 OpenDroneID 标准
  2. 修复精度值：HorizAccuracy 从 4(1.852km) 改为 11(<3m)，SpeedAccuracy 从 2(<3m/s) 改为 4(<0.3m/s)，BaroAccuracy 设为 Unknown(0)
  3. 高度仍显示"未知"、操作员 ID 仍显示"未知"、描述显示"-" — 可能是接收软件解析问题，需进一步测试验证
- 垂直速度与高度变化不合理：将高度波动幅度从 ±0.5m 增大到 ±5m（45-55m），垂直速度从 ±0.1 增大到 ±0.5 m/s，使导数关系 d/dt[5*sin(0.1t)] = 0.5*cos(0.1t) 一致
- 无人机标识字段修改：
  1. 无人机型号（UAS ID / Basic ID）：填写为 "ESP32S3"
  2. 无人机名字（Self-ID 描述）：前缀 "CRID-" + MAC 后 4 位（如 "CRID-3457"）
  3. 飞手名字（Operator ID）：前缀 "OP-CAAC-" + MAC 后 4 位（如 "OP-CAAC-3457"）
  4. 在 cn_crid_config_t 中增加 drone_name 字段
  5. MAC 地址从 esp_efuse_mac_get_default() 动态获取，替代硬编码
  6. Self-ID 报文描述字段改为填充 drone_name（原为 operator_id）
  7. ID Type 从 CAA_REGISTRATION 改为 SERIAL_NUMBER
  8. SSID 也使用 MAC 后 4 位后缀（如 "CN-CRID-3457"）
- 根据接收端截图进一步修复：
  1. 修复高度累积 bug：crid_patrol.c 中 new_alt_msl 从 `config->altitude_msl + alt_offset` 改为 `config->base_altitude_msl + alt_offset`，并在结构体中增加 base_altitude_msl 字段
  2. 修复航向计算：从 atan2f(dy, dx) 改为 atan2f(dlon, dlat)，使航向以正北为 0° 顺时针增加
  3. 航向编码从 0.5°/LSB 改回 1°/LSB（适配此接收端按 1°/LSB 解码）
  4. 垂直速度 31.5 m/s 问题：经分析是接收端解码公式有误（未做 -63 偏移），建议保持标准编码不变
