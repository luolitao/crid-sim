# 2026-06-01

## 修正：无人机型号与 ID 参数对调

- **问题**：`uas_id` 填了 "ESP32S3"（型号），`drone_name` 填了 "CRID-XXXX"（名字），两者语义错误
- **修正**（`crid_config.c`）：
  - `uas_id`（Basic ID 报文）→ "CRID-XXXX"（无人机唯一标识/序列号）
  - `drone_name`（Self-ID 描述报文）→ "ESP32S3"（无人机型号）
- 同步更新了 `crid_config.h` 的字段注释和日志输出描述
